import React from 'react';
import ReactDOM from 'react-dom/client';
import BankStatementAnalyzer from './app/BankStatementAnalyzer';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <BankStatementAnalyzer />
  </React.StrictMode>
);
